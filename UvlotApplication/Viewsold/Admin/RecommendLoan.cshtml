
@{
    ViewBag.Title = "CreateUser";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<div class="charts">
    <div class="mid-content-top charts-grids">
        <div class="middle-content">
            <!--<h4 class="title">Create User</h4>-->
            <!-- Mega Form Starts -->
            <section class="section-padding contact-us-padding form-padding">
                        
                <div class="tables">
                    <h2 class="title1">Recommend Loan</h2>
                    <div class="table-responsive bs-example widget-shadow">
                        @*<h4>Users:</h4>*@
                        @{int i = 0;}
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>No</th>
                                    <th>Name</th>
                                    <th>Phone</th>
                                    <th>Email</th>
                                    <th>ContactAddress</th>
                                    <th>Loan Amount</th>
                                    <th>Date</th>
                                    <th>View</th>
                                </tr>
                            </thead>
                            <tbody>
                            @if (ViewBag.Data == null)
                                {
                                <h2>No Record Found</h2>
                                 }
                                 else
                                 {
                                 foreach (var Loan in ViewBag.Data)
                                 {
                            <tr>
                           

                                <td scope="row">@(++i)</td>
                                <td>@Loan.Surname + @Loan.Firstname</td>
                                <td>@Loan.PhoneNumber</td>
                                <td>@Loan.EmailAddress</td>
                                <td>@Loan.ContactAddress</td>
                                <td>@Loan.LoanAmount</td>
                                <td>@Loan.ValueDate</td>
                                <td>
                                    <a onclick="return confirm('Are You Sure?')" href="@Url.Action("RecommendLoan", "Admin", new { id = @Loan.ID })">View</a>
                                </td>
                            </tr>
                            }
                            }
                            </tbody>
                        </table>
                    </div>
                </div>
            </section>

            <!-- // Mega Form Ends-->

        </div>

    </div>
</div>

